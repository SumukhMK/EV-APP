/**
 * Cross-module plumbing: the paged response every list returns, the error body
 * every failure returns, and the exceptions that produce it.
 *
 * <p>Nothing domain-specific belongs here. If a class knows what a bike is, it
 * belongs in the module that owns bikes.
 */
package com.evrental.common;
